<form method="post" action="{{ path('app_employee_sentiments_delete', {'id': employee_sentiment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee_sentiment.id) }}">
    <button class="btn">Delete</button>
</form>
