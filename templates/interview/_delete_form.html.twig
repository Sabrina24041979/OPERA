<form method="post" action="{{ path('app_interview_delete', {'id': interview.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interview.id) }}">
    <button class="btn">Delete</button>
</form>
