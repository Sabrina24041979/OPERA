{% extends 'base_bo.html.twig' %}

{% block title %}Nouvelle Action{% endblock %}

{% block body %}
    <h1>Ajouter une action</h1>

    <div class="d-flex justify-content-end">
		 <a href="{{ path('app_interview_show', {'id': app.request.query.get('interviewId')}) }}" class="btn btn-success">Retour</a>
	</div>

    {# Inclusion du formulaire #}
    {{ include('action/_form.html.twig') }}

    {# Lien pour retourner à la liste des entretiens #}
    
{% endblock %}
