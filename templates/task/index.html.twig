{% extends 'base_bo.html.twig' %}

{% block title %}Tasks{% endblock %}

{% block body %}
    <h1>Tasks</h1>
    <a href="{{ path('task_new') }}" class="btn btn-success">Add New Task</a>
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Assignée à</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for task in tasks %}
            <tr>
                <td>{{ task.id }}</td>
                <td>{{ task.name }}</td>
                <td>{{ task.user.username }}</td>
                <td>
                    <a href="{{ path('task_show', {'id': task.id}) }}" class="btn btn-info">Voir</a>
                    <a href="{{ path('task_edit', {'id': task.id}) }}" class="btn btn-primary">Editer</a>
                    {{ include('task/_delete_form.html.twig', {'task': task}) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucune tâche trouvée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
