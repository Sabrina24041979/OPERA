<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('home') }}">
                <img src="{{ asset('/image/logo_OPERA.png') }}" alt="Logo OPERA" style="width: 100px;">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_notre_solution') }}">Notre Solution</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_history') }}">Notre Histoire</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_values') }}">Nos Valeurs</a></li>
                </ul>
                <a href="{{ path('app_login') }}" class="btn btn-primary">Connexion</a>
            </div>
        </div>
    </nav>
</header>
