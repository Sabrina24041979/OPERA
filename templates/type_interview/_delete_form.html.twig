<form method="post" action="{{ path('app_type_interview_delete', {'id': id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
    <button class="btn">Delete</button>
</form>
