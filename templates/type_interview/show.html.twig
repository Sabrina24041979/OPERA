{% extends 'base.html.twig' %}

{% block title %}TypeInterview{% endblock %}

{% block body %}
    <h1>TypeInterview Details</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ type_interview.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_interview.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_interview.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_interview_index') }}" class="btn btn-primary">Back to list</a>
    <a href="{{ path('app_type_interview_edit', {'id': type_interview.id}) }}" class="btn btn-secondary">Edit</a>
    {{ include('type_interview/_delete_form.html.twig', {'id': type_interview.id}) }}
{% endblock %}
